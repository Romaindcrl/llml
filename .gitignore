# Models (large — download separately; see README)
models/
*.safetensors
*.gguf

# Python env
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Runtime artifacts / logs / adapters / data
logs/
bench/out/
*.jsonl
.env

# Secrets
.env
.env.local
.webui_secret_key
*secret_key*
*.key

# OS
.DS_Store

# eval runpod state (contains exec token)
eval/runpod/.pod.json
eval/runpod/.pod_*.json

# Lot 2 live scoreboard — snapshots transitoires (résultats finaux committés séparément)
results/raw/lot2/tally_*.json
